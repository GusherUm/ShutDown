{
  "name": "shutdown-timer-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder"
  },
  "devDependencies": {
    "electron": "^25.0.0",  
    "electron-builder": "^24.0.0"  
  },
  "build": {
    "appId": "com.shutdown.timer",
    "productName": "Shutdown Timer App",
    "win": {
      "target": "nsis"
    }
  }
}